import { VideoCard } from '../../components/video-card';

const HomePage = () => {
  return (
    <div style={{ margin: `24px 0` }}>
      <VideoCard />
      <VideoCard />
    </div>
  );
};

export default HomePage;
